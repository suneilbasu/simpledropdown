<h1>Students#index</h1>

<%= form_tag students_path, :method => 'get' do %>
	<%= text_field_tag :search, params[:search] %>
	<%=submit_tag "Search" %>
<% end %>

<% @students.each do |n| %>
  <li>
  <%= n.fname %>

  <%= n.sname %>

  <%= n.tutor.name %>

  </li>
<% end %>
